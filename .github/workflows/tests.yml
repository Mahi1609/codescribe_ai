# name: Run CodeScribe AI Tests

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]
#   workflow_dispatch:

# jobs:
#   test:
#     runs-on: ubuntu-latest

#     steps:
#     - name: 📥 Checkout repo
#       uses: actions/checkout@v3

#     - name: 🐍 Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: 3.10

#     - name: 📦 Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt

#     - name: 🧪 Run all unit + integration tests
#       run: |
#         python -m unittest discover tests

#     - name: ✅ Upload README (optional)
#       if: success()
#       uses: actions/upload-artifact@v3
#       with:
#         name: Generated-Docs
#         path: docs/README.md



name: CI - Tests & Coverage

on:
  push:
    branches:
      - main
      - dev
  pull_request:

jobs:
  test:
    name: Test on Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11, 3.12]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run unit tests with coverage
        run: |
          pytest tests --cov=core --cov=scripts --cov-report=xml --cov-report=term-missing

      - name: Run integration test (pipeline execution)
        run: |
          python scripts/run_pipeline.py examples/sample_project docs/README.md
          test -f docs/README.md

      - name: Upload coverage to Codecov
        if: success()
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: true
